# Supabase CLI (local migrations / deploy only — not used by the React app)
SUPABASE_ACCESS_TOKEN=<your-supabase-cli-access-token>

# Set in Supabase Dashboard → Edge Functions → Secrets (not in Vercel):
# WEATHERAPI_KEY=<your-weatherapi.com-key>
