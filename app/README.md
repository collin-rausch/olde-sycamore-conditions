# React app (signage)

This folder is the deployable web app. **Root Directory** for Vercel should be `app`.

See the repository root **[README.md](../README.md)** and **[docs/CLIENT_HANDOFF.md](../docs/CLIENT_HANDOFF.md)** for full setup.

```bash
cp .env.example .env.local
npm install
npm start
```
